# Static Forms API key. Get this from your Static Forms account dashboard.
# The key is embedded in the static HTML at build time and is not a server secret.
NEXT_PUBLIC_STATIC_FORMS_API_KEY=replace_with_static_forms_api_key
